//
//  ContentView.swift
//  Root surface. For Phase 0 this is just the tuner — the simplest use of the
//  listening engine and the best way to validate detection before any UI.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        TunerView()
    }
}

#Preview {
    ContentView()
}
